package oops_assignments;

/**
 * Interface class that the heap iterator class will implement to check if the
 * heap has next value and also access the next value in the heap
 */

public interface Iterator {
	public boolean hasNext();

	public Object next();

}
